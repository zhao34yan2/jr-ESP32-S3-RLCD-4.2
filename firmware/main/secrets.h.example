#ifndef SECRETS_H
#define SECRETS_H

/*
 * ⚠️ 复制此文件为 secrets.h 并填入你的配置
 * secrets.h 已被 .gitignore 排除，不会提交到仓库
 */

/* WiFi */
#define WIFI_SSID       "YourWiFiSSID"
#define WIFI_PASSWORD   "YourWiFiPassword"

/* 桥接服务器 (DeepSeek bridge 地址) */
#define BRIDGE_URL      "http://192.168.x.x:7777/api/usage"
#define BRIDGE_TOKEN    ""

/* 和风天气 API Key (可选，用于更精确的室外天气) */
#define QWEATHER_API_KEY    ""

/* 纬度/经度 (open-meteo 使用，免 Key) */
#define WEATHER_LAT     "31.2304"
#define WEATHER_LON     "121.4737"
#define WEATHER_CITY    "Shanghai"

/* 轮询间隔 (秒) */
#define POLL_INTERVAL_SEC   60

#endif /* SECRETS_H */